while wait(.1) do
game.Workspace.NAMEHERE.HumanoidRootPart.CFrame = CFrame.new(Vector3.new(game.Workspace.Lobby.ObbyReward.Position.X, game.Workspace.Lobby.ObbyReward.Position.Y, game.Workspace.Lobby.ObbyReward.Position.Z))
end